<!-- For color swatch like the Jack Archer Anytime Tees.
Replace the existing radio or swatch code with the following Shopify Liquid code.
Keep the existing class of radio or swatch and replace the Shopify Liquid code
that starts with the "assign" keyword.

You can create an "If Product.tags" statement with this code and ensure you create your
own class along with CSS to maintain the branding of the brand.

Additionally, you need to create another Shopify Liquid file named "color-swatch-style".
Please copy all the code from "color-swatch-style.liquid" to ensure this code works properly.
Link here: https://github.com/KimOpenStore/OpenStore-Custom-Section/blob/main/Jack%20Archer/Color%20Swatch/color-swatch-style.liquid

For more details, please contact Kim Pomento on 06/01/2023.
----->
  
  
  {%- assign color_swatch_config = settings.color_swatch_config | newline_to_br | split: '<br />' -%}
                              {%- for value in option.values -%}
                                {%- assign downcase_value = value | downcase -%}
                  
                                <li class="HorizontalList__Item">
                                  <input id="option-{{ section.id }}-{{ forloop.parentloop.index0 }}-{{ forloop.index0 }}" class="ColorSwatch__Radio" type="radio" name="option-{{ forloop.parentloop.index0 }}" value="{{ value | escape }}" {% if value == option.selected_value %}checked="checked"{% endif %} data-option-position="{{ option.position }}">
                                  <label for="option-{{ section.id }}-{{ forloop.parentloop.index0 }}-{{ forloop.index0 }}" class="ColorSwatch ColorSwatch--large {% if downcase_value == 'white' %}ColorSwatch--white{% endif %}" data-tooltip="{{ value | escape }}" style="{% render 'color-swatch-style', color_swatch_config: color_swatch_config, value: value %}">
                                    <span class="u-visually-hidden">{{ value }}</span>
                                  </label>
                                </li>
                                {% endfor %}
                              </fieldset>
                            {% else %}
                              <select class="single-option-selector__radio single-option-selector-{{ section.id }} product-form__input" id="SingleOptionSelector-{{ forloop.index0 }}" data-index="option{{ forloop.index }}">
                                {% for value in option.values %}
                                  <option value="{{ value | escape }}"{% if option.selected_value == value %} selected="selected"{% endif %}>{{ value | escape }}</option>
                                {% endfor %}
